main(List<String> args){
  String name = "aysegul";
  String surName = "yilmaz";
  var course = 'Dart\'s Uses';
  String courseinformation = " We will learn dart and flutter";
  
  print(name + " " + surName);
  print("$name $surName");
  print(" is in my surname $surName character number: " + surName.length.toString());
  print("Character number ${surName.length}");
  
  double area =10;
  double height = 12;
  
  print("Area ${area.toInt()} and height ${height.toInt()} Area of the rectangle ${area.toInt()*height.toInt()}");
  
  
}